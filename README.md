# Job Search OS — public deploy shell (auto-generated)

Built by `npm run build:shell`. PII-free and secular: no personal data, no owner identity, no scripture.
Push this folder to a separate repo and point Vercel at it (env vars in CLOUD-SETUP.md). Do NOT hand-edit; re-run the build.
